
Task log output should stop auto scrolling when you scroll up...

Abillity to add multiple channel urls for a single channel
Select multiple videos and do bulk actions such as (delete, refresh, etc...)

Add a setting in the config that downloads the thumbnails into a folder instead of the sqlite database
Deleting a video should delete the downloaded thumbnail (Don't delete thumbnail if other videos are using the same thumbnail hash)

Tasks tab looks UGLY 🤮🤮🤮 (make it look pretty)

Store a history for videos

Automatically update yt-dlp binary (if possible)

Download live stream chat in real time

Fix pagination performance, right now it's shit while having 10,000+ videos in a database...

Use yt-dlp as a fallback when ytarchive doesn't exist... (use --live-from-start, YUCK, that shi has never worked for me...)
